name: test

on:
  pull_request:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Setup Flutter
        uses: subosito/flutter-action@v1
        with:
          channel: 'stable'

      - run: dart pub get

      - run: dart analyze

      - run: flutter test --coverage

      - run: cd example && dart test --coverage coverage

      - run: dart pub global activate coverage

      - run: format_coverage --packages=.dart_tool/package_config.json --report-on=lib --in=coverage/test/router_test.dart.vm.json --lcov --out=coverage/test/router_test_lcov.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          file: coverage/router_test_lcov.info